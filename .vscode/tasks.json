{
  "version": "2.0.0",
  "// Everything passes --locked": "A build must never silently rewrite Cargo.lock; see the justfile.",
  "tasks": [
    {
      "label": "check",
      "detail": "fmt --check, clippy -D warnings, and the test suite. The pre-commit gate.",
      "type": "shell",
      "command": "cargo fmt --all -- --check && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo test --locked --workspace",
      "group": { "kind": "test", "isDefault": true },
      "problemMatcher": ["$rustc"],
      "presentation": { "panel": "dedicated", "clear": true }
    },
    {
      "label": "build",
      "detail": "Debug build",
      "type": "shell",
      "command": "cargo build --locked --workspace",
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$rustc"]
    },
    {
      "label": "build (release)",
      "detail": "What the player runs. Asset decode is unusably slow in a debug build.",
      "type": "shell",
      "command": "cargo build --locked --release --workspace",
      "group": "build",
      "problemMatcher": ["$rustc"]
    },
    {
      "label": "test",
      "type": "shell",
      "command": "cargo test --locked --workspace",
      "group": "test",
      "problemMatcher": ["$rustc"]
    },
    {
      "label": "clippy",
      "type": "shell",
      "command": "cargo clippy --locked --workspace --all-targets -- -D warnings",
      "group": "build",
      "problemMatcher": ["$rustc"]
    },
    {
      "label": "fmt",
      "type": "shell",
      "command": "cargo fmt --all",
      "problemMatcher": []
    },
    {
      "label": "audit",
      "detail": "Supply-chain gate: advisories, licences, banned crates, source registries.",
      "type": "shell",
      "command": "cargo deny --locked check",
      "problemMatcher": []
    },

    {
      "label": "dist: windows",
      "detail": "Cross-compile daysengine.exe and package it with its DLLs. Builds the vendored SDL3 and ffmpeg first if needed.",
      "type": "shell",
      "command": "./tools/build-sdl.sh windows && ./tools/build-ffmpeg.sh windows && ./tools/dist.sh windows",
      "group": "build",
      "problemMatcher": ["$rustc"],
      "presentation": { "panel": "dedicated" }
    },
    {
      "label": "deps",
      "detail": "Build the vendored SDL3 and ffmpeg every build links. Slow; once per checkout. Nothing compiles until this has run.",
      "type": "shell",
      "command": "./tools/build-sdl.sh linux && ./tools/build-ffmpeg.sh linux",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated" }
    },
    {
      "label": "windows: deps",
      "detail": "Build the vendored SDL3 and ffmpeg for MinGW-w64. Slow; once per checkout.",
      "type": "shell",
      "command": "./tools/build-sdl.sh windows && ./tools/build-ffmpeg.sh windows",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated" }
    },
    {
      "label": "dist: linux",
      "detail": "Release archive with the vendored SDL3 and LGPL ffmpeg beside the binaries.",
      "type": "shell",
      "command": "./tools/build-sdl.sh linux && ./tools/build-ffmpeg.sh linux && ./tools/dist.sh linux",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated" }
    },
    {
      "label": "dist: windows (binaries already built)",
      "detail": "Package only. Fails if the vendored libraries or the .exe files are missing.",
      "type": "shell",
      "command": "./tools/dist.sh windows",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated" }
    },

    {
      "// The inspection subcommands": "These are the verification path. They run headless against a real install and print what they found; the game itself is not a test harness. The same binary with no subcommand opens a window instead.",
      "label": "inspect: menu",
      "detail": "Drive the menu state machine headlessly and report where each event lands.",
      "type": "shell",
      "command": "cargo run --locked --release --bin daysengine -- --game \"${input:gameDir}\" menu --events \"${input:menuEvents}\"",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated", "clear": true }
    },
    {
      "label": "inspect: menu --check-all",
      "detail": "Open every screen of the install and report which ones this engine can draw.",
      "type": "shell",
      "command": "cargo run --locked --release --bin daysengine -- --game \"${input:gameDir}\" menu --check-all",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated", "clear": true }
    },
    {
      "label": "inspect: ui",
      "type": "shell",
      "command": "cargo run --locked --release --bin daysengine -- --game \"${input:gameDir}\" ui",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated", "clear": true }
    },
    {
      "label": "inspect: save",
      "type": "shell",
      "command": "cargo run --locked --release --bin daysengine -- --game \"${input:gameDir}\" save",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated", "clear": true }
    },
    {
      "label": "inspect: scripts",
      "detail": "Parse every .ORS and report anything the parser cannot handle.",
      "type": "shell",
      "command": "cargo run --locked --release --bin daysengine -- --game \"${input:gameDir}\" scripts --stats",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated", "clear": true }
    },
    {
      "label": "inspect: assets",
      "detail": "Check that every asset path referenced by every script resolves.",
      "type": "shell",
      "command": "cargo run --locked --release --bin daysengine -- --game \"${input:gameDir}\" assets",
      "problemMatcher": [],
      "presentation": { "panel": "dedicated", "clear": true }
    }
  ],

  "inputs": [
    {
      "id": "gameDir",
      "type": "promptString",
      "description": "Game directory (the one holding Packs and the game executable)",
      "default": "${env:DAYS_GAME_DIR}"
    },
    {
      "id": "menuEvents",
      "// How a screen is reached": "`menu` starts at the title and is driven by events, because that is how a player reaches a screen. There is no mode argument: the state machine decides, which is the thing being verified.",
      "type": "promptString",
      "description": "Menu events, comma separated (down, up, enter, esc, click:X:Y, ...). Empty for the title as it opens.",
      "default": ""
    }
  ]
}
