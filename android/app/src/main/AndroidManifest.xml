<?xml version="1.0" encoding="utf-8"?>
<!--
    No permissions. The player's install is reached through the Storage Access
    Framework: what they grant in the system folder picker is a tree, and the
    grant itself is the authority to read it. Asking for storage permission on
    top of that would be asking for every file on the device in order to read
    the one folder they already chose.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:glEsVersion="0x00020000" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <uses-feature android:name="android.hardware.type.pc" android:required="false" />

    <!-- The control bar's rumble, which src/main's Pads already drives. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/Theme.DaysEngine"
        android:hardwareAccelerated="true"
        android:enableOnBackInvokedCallback="false">

        <!--
            Back reaches the engine instead of finishing the activity. SDL
            then reports it as the AC_BACK key, which install::binding binds
            to Cancel beside Escape, so it backs out of a menu the way the
            same press does on a desktop.
        -->
        <meta-data android:name="SDL_ENV.SDL_ANDROID_TRAP_BACK_BUTTON" android:value="1" />

        <activity
            android:name=".PickerActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DaysEngineActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:preferMinimalPostProcessing="true"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation" />
    </application>
</manifest>
